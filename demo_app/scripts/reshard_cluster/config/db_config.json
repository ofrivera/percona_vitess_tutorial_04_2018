{
    "sidecar_dbname": "_vt",
    "init_file": "init_db.sql",
    "dbconfig": {
        "dba": {
            "password": "",
            "user": "vt_dba",
            "permissions": "ALL"
        },
        "app": {
            "password": "",
            "user": "vt_app",
            "permissions": "SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, RELOAD, PROCESS, FILE, REFERENCES, INDEX, ALTER, SHOW DATABASES, CREATE TEMPORARY TABLES, LOCK TABLES, EXECUTE, REPLICATION SLAVE, REPLICATION CLIENT, CREATE VIEW, SHOW VIEW, CREATE ROUTINE, ALTER ROUTINE, CREATE USER, EVENT, TRIGGER"
        },
        "global": {
            "charset": "utf8",
            "dbname": "vt_cell1_keyspace"
        },
        "allprivs": {
            "password": "",
            "user": "vt_allprivs",
            "permissions": "SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, RELOAD, PROCESS, FILE, REFERENCES, INDEX, ALTER, SHOW DATABASES, CREATE TEMPORARY TABLES, LOCK TABLES, EXECUTE, REPLICATION SLAVE, REPLICATION CLIENT, CREATE VIEW, SHOW VIEW, CREATE ROUTINE, ALTER ROUTINE, CREATE USER, EVENT, TRIGGER"
        },
        "repl": {
            "password": "",
            "user": "vt_repl",
            "permissions": "REPLICATION SLAVE"
        },
        "filtered": {
            "password": "",
            "user": "vt_filtered",
            "permissions": "SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, RELOAD, PROCESS, FILE, REFERENCES, INDEX, ALTER, SHOW DATABASES, CREATE TEMPORARY TABLES, LOCK TABLES, EXECUTE, REPLICATION SLAVE, REPLICATION CLIENT, CREATE VIEW, SHOW VIEW, CREATE ROUTINE, ALTER ROUTINE, CREATE USER, EVENT, TRIGGER"
        }
    },
    "db_types": [
        "dba",
        "allprivs",
        "app",
        "repl",
        "filtered"
    ],
    "vars": {}
}